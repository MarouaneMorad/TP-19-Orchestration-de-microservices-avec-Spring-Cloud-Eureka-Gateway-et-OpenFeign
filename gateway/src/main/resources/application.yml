# Static gateway routes moved to app.yml (archived)
# Renamed to app.yml to allow dynamic routing via Eureka (lb://SERVICE-NAME)
# Original static routes were:
# spring:
#   cloud:
#     gateway:
#       routes:
#         - id: r1
#           uri: "http://localhost:8088"
#           predicates:
#             - Path=/clients/**
#         - id: r2
#           uri: "http://localhost:8088"
#           predicates:
#             - Path=/client/**
